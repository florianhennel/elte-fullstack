{
  "tasks": {
    "up":"docker compose up -d",
    "down": "docker compose down --volumes",
    "dev": "docker compose run --rm deno-api deno run --allow-env --allow-sys --allow-read --allow-net --watch main.ts",
    "seed": "docker compose run --rm deno-api deno run --allow-env --allow-sys --allow-read --allow-net --allow-write seed.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "bcryptjs": "npm:bcryptjs@^2.4.3",
    "dotenv": "npm:dotenv@^16.4.5",
    "express": "npm:express@^5.0.0",
    "jsonwebtoken": "npm:jsonwebtoken@^9.0.2",
    "mongoose": "npm:mongoose@^8.7.0"
  }
}
